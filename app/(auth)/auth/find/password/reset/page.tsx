import { Wrapper } from '@/components/wrapper';

import ResetPassword from './ResetPassword';

export default function FindPasswordPage() {
  return (
    <Wrapper.MAIN text="비밀번호 번호 변경">
      <ResetPassword />
    </Wrapper.MAIN>
  );
}
