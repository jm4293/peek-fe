import { Text } from '@/components/text';
import { Wrapper } from '@/components/wrapper';

export default function Chart() {
  return (
    <Wrapper.SECTION text="지수">
      <Text.PARAGRAPH text="코스피" />
    </Wrapper.SECTION>
  );
}
